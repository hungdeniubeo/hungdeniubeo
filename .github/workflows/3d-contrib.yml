name: Legacy 3D Contribution Workflow (Disabled)

# This workflow is intentionally disabled.
# Snake + 3D profile assets are now generated together by snake.yml
# so the two workflows no longer overwrite the output branch.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Profile assets are generated by .github/workflows/snake.yml"
